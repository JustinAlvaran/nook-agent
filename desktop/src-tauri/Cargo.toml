[package]
name = "nook-desktop"
version = "0.0.0"
description = "Phase 2 scaffold for the Nook Windows companion"
edition = "2021"

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = [] }

[features]
default = []
