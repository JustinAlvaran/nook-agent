# Feedback and reflection

Task results accept structured positive or negative feedback with bounded categories and optional corrections. Feedback is owner-scoped and never becomes permanent memory automatically. A correction may later create a separate proposal that still requires user approval.

Verified final completion stores a safe operational reflection: outcome, what worked, failures, corrections, and reusable preference candidates. This is not chain-of-thought and contains no private reasoning.
